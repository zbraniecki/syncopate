[package]
name = "syncopate"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbeam = "0.8"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
